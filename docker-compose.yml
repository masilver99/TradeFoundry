services:
  tradefoundry:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Storage__DataDirectory: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
